# ---- Gradle / 构建产物 ----
.gradle/
build/
app/build/
.kotlin/
captures/
*.log

# ---- 本机专属配置（含绝对路径，不入库） ----
local.properties

# ---- IDE ----
.idea/
*.iml
*.hprof
.DS_Store

# ---- 手工留存的中间数据（数据库副本 / 演示灌库产物） ----
seed/

# ---- 本机数据库备份与导出的中间产物 ----
# 里面是真实笔记数据，绝不能进公开仓库（seed/ 已在上面忽略）
lg-backup/
seed/
*.db
*.db-wal
*.db-shm
tools/*.png

# ---- 开发过程中的真机/模拟器截图 ----
# 这些是本地验证时随手截的，含设备标识、同步共享密钥、局域网地址与本机笔记内容，
# 不进公开仓库。要给 README 用的截图放在 docs/screenshots/（脱敏后）。
lg-shots/
demo-shots/

# 注意：以下内容**必须**入库，不要加进 ignore
#   gradle/wrapper/gradle-wrapper.jar   —— 没有它 clone 下来跑不了 ./gradlew
#   gradle/wrapper/gradle-wrapper.properties
#   app/schemas/**                      —— Room 导出的 schema，迁移要靠它比对
#   docs/screenshots/**                 —— README 引用的界面截图（已脱敏）

# 本机验证用的原始截图（只提交 docs/screenshots/ 里的脱敏图）
shots*/
